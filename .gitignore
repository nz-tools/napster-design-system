# macOS junk
.DS_Store
._*

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# OS / archives
Thumbs.db
*.zip
# v1.1.0: Avantt retired from the design system. No font bundle to ship —
# the three Google Fonts (Inter, Instrument Serif, IBM Plex Mono) load
# from Google's CDN via colors_and_type.css. No exception to the *.zip
# rule is needed any more.
